(executable
 (name binding)
 (modules binding)
 (libraries facile)
 (link_flags
  (:include flags.sexp))
 (modes
  (native object))
 (promote
  (until-clean)
  (into .)))

(rule
 (targets flags.sexp)
 (deps
  (:discover config/discover.exe))
 (action
  (run %{discover})))
